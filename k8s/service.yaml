apiVersion: v1
kind: Service
metadata:
  name: openfinance-mcp
  namespace: fintech
  labels:
    app: openfinance-mcp
spec:
  type: ClusterIP
  selector:
    app: openfinance-mcp
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
